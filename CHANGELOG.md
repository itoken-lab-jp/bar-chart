# 変更履歴

## 1.32.1.0（2026-09-26）

- カテゴリが 1,000 件を超えても、全部読み込むようにした。これまでは先頭の 1,000 件だけを読み、「その他」の合計・積み上げの合計・パレートの累積比が、読めた分だけで計算されていた（1,500 カテゴリで「その他」が本当より 500 少なく出た。Power BI は右上の小さな「i」で「値が多すぎます」と知らせるだけだった）。30,000 件ずつ読み、続きを読んでいるあいだは左下に「続きのカテゴリを読み込んでいます…」と出す。Power BI の読み込みの上限（100 MB）で読み切れないときは、警告のアイコンで知らせ、読めた分で描く
- 「データ ラベル > 値」に「符号の色」を足した（標準に無い項目）。色なし（既定）・マイナスだけ・プラスとマイナスから選ぶと、データ ラベルの値の文字を「マイナスの色」（既定 #D64554）・「プラスの色」（既定 #1AAB40）で塗る。色は見えている符号で決める（丸めて 0 になるマイナスを ▲0 のように符号を残して出すときはマイナスの色、±0・0 は塗らない）。棒の外のラベルと、背景を付けたラベルに効く（棒の中のラベルは、今までどおり棒の色に合わせて読める色を自動で選ぶ）。詳細の行（割合など）は塗らない。既定では今までと同じ色

## 1.31.0.0（2026-09-26）

- 「データ ラベル > 値」に、マイナスと 0 の書き方を足した（標準に無い項目）。マイナスは -1,234（既定）・▲1,234・△1,234・(1,234)、0（丸めて 0 になる値も）は 0（既定）・±0・- から選ぶ。「丸めて 0 のマイナスに符号」をオン（既定）にすると、丸めて 0 になるマイナスは ▲0 のように符号を残し、オフにすると 0 の書き方にそろえる。単位の語は記号の内側に入る（▲1.2億、(1.2億)）。決算説明資料などの書き方に合わせるためで、既定では今までと同じ表示
- Y 軸（第 2 Y 軸も）のタイトルに単位を入れた（「タイトル > スタイル」が「単位のみを表示」「両方を表示」）ときは、単位のラベルを出さないようにした。これまでは「売上高 (百万円)」のタイトルと「(百万円)」のラベルの 2 か所に同じ単位が出た。タイトルを消していれば、今までどおり単位のラベルを出す
- データ ラベルに背景を出したとき、自動の文字色を背景の色で決めるようにした（背景に透過性があれば、下の棒の色や白と混ぜた色で決める）。これまでは背景を出すと濃い灰色に固定され、濃い背景の上で読めなかった。背景の既定（白・透過性 0%）では今までと同じ色
- Y 軸（第 2 Y 軸も）の最小値・最大値に「1,000」のように桁区切りのカンマを入れても読むようにした。これまでは読めずに自動の範囲になっていた
- ダッシュボードのタイルなど、操作を受け付けない場所では、棒・凡例を押しても選択を送らず、右クリックのメニューも出さないようにした
- 棒の色が 8 桁（#RRGGBBAA）のとき、透明度を外して読み、データ ラベルの自動の文字色を決めるようにした。これまでは桁を読み違え、黄色の棒に白い文字を選ぶことがあった

## 1.30.1.0（2026-09-25）

- ドリルダウンしたとき、今いる位置（「FY26 ＞ H1」「事業A ＞ 製品A1」など）をグラフの左上に出すようにした（標準に無い項目）。縦棒では単位のラベルと同じ行に、描く範囲の左端から出す。横棒では上に 1 行とる（「プロット エリアの右上」の単位があれば、同じ行の左に並べる）。棒の上にはみ出したデータラベル・合計ラベルとは重ねず、その手前で省く。低い図で、位置を出すと描く範囲が潰れるときは出さない。上の階層のうち、どのカテゴリでも同じ値をつなぐので、絞り込みで上の階層が 1 つに決まった「すべて展開」でも出る。ドリルしていないとき・「すべて展開」で親が混ざるときは出さない。空白の値は「(空白)」と出す。入りきらなければ末尾を「…」で省き、マウスを乗せると全部見られる。「グラフの種類」カードの「ドリルの位置」で消せる（既定はオン）
- どのカテゴリでも同じ上の階層は、X 軸の段に繰り返さないようにした（ドリルダウンすると、どの棒の下にも同じ親の段が 1 本出ていた）。ツールヒントと、読み上げの名前には今までどおり上の階層から全部出す。「ドリルの位置」をオフにしても段には出さない

## 1.29.17.0（2026-09-25）

- X 軸の「高さの最大値 (%)」（横棒は「最大幅 (%)」）が、レポートのテーマに従うようにした。標準と同じ名前（`maxMarginFactor`）でテーマが決めている値を、書式ペインで変えていないときの既定にする。新しい既定のテーマ（Fluent 2）は 50 で、標準と同じく、25 のときより長く名前を出す。テーマが何も決めていなければ今までどおり 25。上限を大きくしても（標準は 50 まで、自作は 100 まで入る）、描く範囲に高さ（横棒は幅）の 4 分の 1 は残し、名前は「…」で省く（名前の欄に 1 文字と「…」も入らないほど小さい図では、名前を出さない。上の余白やパレートのランクの帯だけで 4 分の 3 を使い切るほど小さい図では、4 分の 1 は残らない）。これまでは 100 にすると名前の欄が棒を押しつぶし、名前がビジュアルの下にはみ出して頭が切れた
- 「Y 軸 > 範囲 > 目盛りの本数 (目安)」を足した。空なら自動（描く範囲の長さで決める。標準と同じ）。数を入れると、その本数以内で切りのいい目盛り（グリッド線）にする。スライサーでデータが変わっても間隔が追従する。数字が重なるほど多い本数は、重ならない本数まで減らす（自動のときも同じ）。第 2 Y 軸も同じ上限。対数の軸では効かない（目盛りは 10 の累乗で決まる）
- 階層の上のレベルの区切り（囲み・線で区切った段）を押すか Enter・Space で、その区切りのカテゴリをまとめて選ぶようにした（Ctrl で足す）。系列があれば、区切りの中の棒を系列ごと全部選ぶ。ほかのビジュアルも、その区切りで絞り込まれる。パレートのランクの帯と同じ操作。これまでは押しても何も起きなかった。マウスで押したときは、区切り・ランクの帯にフォーカスの枠を出さない（キーボードで移ったときだけ出す）
- 斜めの X 軸の項目名が左端で「…」に省かれないように、はみ出す分だけ棒の並びの前を空けるようにした（ウォーターフォールと同じ考え方）。帯がカテゴリの最小幅を割るほど・縦に立つほどには空けない。極端に長い名前はビジュアルの幅の 30% までで省く。スクロールするとき・縦に立てるときは今までどおり
- X 軸の項目名が斜めでも重なるとき（帯の幅が文字の 1.45 倍を切るとき）は、標準と同じく縦に立てて全部の名前を出すようにした。縦でも重なるほど狭いとき（帯の幅が文字の高さの 1.1 倍を切るとき）は、1 つおき・2 つおき…に間引く（間引いた名前はツールヒントで見られる）。これまでは「カテゴリの最小幅」を 0 にすると、項目が多いと斜めの名前が重なった
- 値の軸の目盛り（グリッド線）の本数を、標準と同じく描く範囲の長さで決めるようにした（縦棒は高さ 150px 未満で 3、300px 未満で 5、それ以上で 8 まで。横棒は幅 300・500px で同じ）。これまでは長さによらず 5 本ほどで、小さい図では標準よりグリッド線が混んでいた。軸の範囲は変わらない。第 2 Y 軸・パレートの累積比の軸も同じ本数の上限にした
- グリッド線と階層の区切り線に crispEdges を掛けないようにした（標準・ウォーターフォールと同じ）。画面の拡大率が整数でない（150% など）と、線ごとに太さが変わっていた
- 斜めの X 軸の項目名を、文字の高さの中ほどを通る線が棒の中心で終わるように置いた。これまでは文字のベースラインの端を棒の中心より 4px 左に置いていたので、文字が左上に寄って見えた
- 軸のタイトルの表示と、凡例の位置・タイトルの表示が、レポートのテーマに従うようにした。標準のビジュアルと同じ名前（`showAxisTitle`・`showTitle`、凡例の位置の `Top`・`Bottom` など）でテーマが決めている値を、書式ペインで変えていない所の既定にする。新しい既定のテーマ（Fluent 2）では軸のタイトルを出さず、凡例は下。ひとつ前の既定（Classic 2026）では軸のタイトルを出す。書式ペインで変えた所は、テーマよりそちらを使う。「既定値にリセット」でテーマの値に戻る
- テーマが何も決めていないときは、X 軸（カテゴリの軸）のタイトルを出さず、Y 軸（数値の軸）のタイトルは出す。凡例は上詰め (左)。横棒でも、カテゴリの軸（縦に並ぶ項目名の軸）の扱いは同じ
- グリッド線の表示・色・線種・幅も、レポートのテーマに従うようにした。標準のビジュアルと同じく、テーマが数値の軸・カテゴリの軸のカードに決めている値（`gridlineShow`・`gridlineColor`・`gridlineStyle`・`gridlineThickness`）を、「グリッド線」カードで変えていない所の既定にする。Fluent 2 では薄い灰（#F0F0F0）の実線、Classic 2026 では今までどおりの点線。「グリッド線」カードの「横」が数値の軸の線、「縦」がカテゴリの軸の線に当たる（横棒でも同じ）。テーマの線種が「カスタム」のときは受け取らず、今の既定のまま
- 系列が 1 本の棒の色を、標準と同じくレポートのテーマのデータの色の 1 番目にした（これまでは #118DFF 固定で、テーマを変えても変わらなかった）。既定のテーマでは 1 番目が #118DFF なので見た目は変わらない。書式ペインで色を変えていれば、そちらを使う。パレートの ABC の色と「その他」の棒の色は、今までどおりそれぞれの設定の色
- 凡例の位置の保存値を標準と同じ値にした。前の版で保存した位置（上詰め (左) など）は、そのまま同じ所に出る
- 前の版で作ったレポートでは、X 軸の「タイトル」を一度も切り替えていないビジュアルは、テーマによってはタイトルが消える（Classic 2026 では出たまま）。出したいときは「X 軸 > タイトル」をオンにする

## 1.26.2.0（2026-09-23）

- 「棒」カードに「その他にまとめる」を足した（標準に無い項目）。「上位の件数」に 1 以上を入れると、値の大きい順にその件数のカテゴリを残し、残りを 1 本の「その他」にまとめていちばん右に置く（値順・逆順の設定によらない）。「名前」と「色」（系列が 1 本のとき）を変えられる。凡例があれば系列ごとに足す
- 「その他」の棒を押すと、まとめたカテゴリを全部選ぶ（ツールヒント・累積比の点も同じ。右クリックのメニューは特定のカテゴリにしない）。「折れ線の値」の線は、その他の所では足しようが無いので空白にする
- パレートでは「その他」も累積比に入れる（まとめたカテゴリの正の値だけを数え、最後で 100% に届く）。累計でも最後に足す
- X 軸の階層を展開しているときは使わない（どのレベルで上位を数えるか決まらないため）
- 前の版で作ったレポートは、そのまま開く（「上位の件数」の既定は 0 で、まとめない）

## 1.25.1.0（2026-09-23）

- 「累計・パレート」カードの「計算」に「パレート」を足した。カテゴリを値の大きい順に並べ（値順・逆順の設定によらない）、累積比の線を右の軸（0〜100%）に重ねる。凡例があれば、カテゴリの合計の大きい順
- 累積比の分母は正の合計で、値が 0 以下のカテゴリは数えない（右端に並べ、累積比の線はそこで止める）。標準のビジュアル計算で組むと、負の値で 100% を超える
- 累積比で A・B・C の 3 ランクに分ける。境目（既定 80% と 95%）は書式で変えられ、境目ちょうどは上のランクに入れる。先頭のカテゴリは必ず A
- 系列が 1 本なら、棒をランクの色で塗る（「ランクで色分け」。A・B・C の色を選べる）。境目の高さに破線を引く（「境目に線を引く」）
- X 軸の下に A・B・C の帯と件数を出す（「ランクの帯」。名前を変えられる）。帯を押すと、そのランクのカテゴリをまとめて選ぶ（Ctrl で足す、もう一度押すと外す）
- ツールヒントに累積比とランクを出す。累積比の線は凡例に出るが、選べない
- パレートのあいだは「折れ線の値」の線を左の軸で描く（右の軸は累積比に使う）。100% 積み上げは積み上げとして描く（どの棒も同じ高さになって意味が無いため）。X 軸の階層を展開していても、親の中ではなく全体を大きい順に並べる
- 横棒では、累積比の軸を上に出し、境目は縦の破線、ランクの帯はカテゴリのラベルの左に出す。帯はキーボード（Enter・Space）でも押せる
- 前の版で作ったレポートは、そのまま開く

## 1.24.1.0（2026-09-23）

- 書式設定に「累計・パレート」カードを足した（標準に無い項目）。「計算」を「累計」にすると、棒の値を並んだ順に足していく。凡例があれば系列ごとに足してから積む。値順で並べ替えているときは、元の値で並べてからその順に足す
- 「区切り」で X 軸の階層のレベルを選ぶと、そのレベルが変わったところで 0 に戻す（年度ごとの累計など）。ドリルアップして区切りのレベルがいちばん下になったら、棒ごとの素の値になる
- 「切り替えボタンを出す」をオンにすると、グラフの右上に「累計」のボタンと区切りの選択を出し、レポートの閲覧者が切り替えられる。押した状態はレポートに保存する。作成者が書式の「計算」や「区切り」を変えたら、閲覧者の切り替えや区切りは捨てる
- 「線」カードに「累計に含める」を足した（既定はオフ）。オンにした線も累計にし、区切りで線を切る
- 累計のときのツールヒントは「値（累計）」と、累計の前の値を並べて出す
- 前の版で作ったレポートは、そのまま開く（「計算」の既定は「なし」）

## 1.23.1.0（2026-09-23）

- X 軸（横棒では Y 軸）の階層に対応した。「カテゴリ」にフィールドを複数入れる（年 → 四半期 → 月、事業 → 製品 など）と、標準と同じくヘッダーの矢印でドリルアップ・ドリルダウン・次のレベルに移動・すべて展開ができる
- すべて展開したときの軸のラベルは、標準と同じく、いちばん下のレベルの下に上のレベルを段に重ね、同じ親が続くところを点線の枠で囲む。軸のタイトル（自動）はいちばん下のレベル名。横棒では、上のレベルをラベルの左の列に出す。斜めに入りきらないラベルは、階層のときは縦に立てる（斜めだと隣の枠をまたぐため。標準と同じ）
- X 軸の「値」に、標準と同じ「ラベルの連結」を足した。オンにすると「FY26 H1 Q1」のように 1 行につなぎ、軸のタイトル（自動）は「FY 半期 四半期」のようにレベル名を並べる
- X 軸の「値」に「階層の見せ方」を足した（標準に無い項目）。「区切り線」（既定。標準と同じ点線）か「囲み」（同じ親のまとまりごとに、角の丸い淡い枠で囲む）を選べる
- 「値順で並べ替え」は、階層では親の中で並べ替える（同じ年の四半期が離ればなれにならない）
- ツールヒントは、階層ではレベルごとに 1 行ずつ出す
- 前の版で作ったレポートは、「カテゴリ」のフィールドが 1 つならそのまま開く。2 つ以上入れていたレポートは、これまで 1 つ目のフィールドの名前だけを出していたが、この版から最初のレベルにまとまって開く（展開すると下のレベルまで出る）

## 1.22.1.0（2026-09-18）

- ビジュアルの表示名を「棒グラフ」から「棒グラフ by itoken lab」にした（視覚化ペインなどに出る名前）。標準の棒グラフと見分けやすくするため。レポートに保存される名前（ビジュアルの識別子・書式の名前）は変えていないので、前の版で作ったレポートはそのまま開ける。見た目と書式の設定は 1.22.0.0 と同じ

## 1.22.0.0（2026-09-17）

- 「線」カードに「段の幅」を足した（標準に無い項目）。補間の種類がステップで「段のつなぎを表示」がオフのときに出る。「カテゴリの間隔」（既定。1.21 までと同じく、隣のカテゴリとのすき間の真ん中まで）か「棒の幅」を選べる。「棒の幅」にすると、値ごとの線をそのカテゴリの棒の端から端まで（集合なら系列の棒のまとまり）の長さで引く。線の太さは「幅」で別に決まるので、前年・予算・平均などを、折れ線（隣のカテゴリとつながる）ではなく棒ごとの線で重ねるのに使える。横棒では棒の厚みの向きに引く。網掛け領域はこれまでと同じ

## 1.21.0.0（2026-09-16）

- グラフの種類と向きを、「列」（いまの「棒」）カードから書式設定のいちばん上の「グラフの種類」カードへ移した。1.14 までに作ったレポートは、そのままの種類と向きで開く
- リボンをグラフの種類から外し、「リボン」カードの見出しのトグルにした（標準の積み上げ縦棒の「リボン」カードと同じ形）。積み上げと 100% 積み上げで出せる。「積む順」は、凡例の順（標準の積み上げ＋リボンと同じ）か値の大きい順（標準のリボン グラフと同じ）。種類を「リボン」にしていたレポートは、積み上げ＋リボン オン＋値の大きい順で開く（見た目は同じ）
- リボンにも「折れ線の値」の折れ線（第 2 Y 軸・線・網掛け領域・マーカー）と合計ラベルを出せるようにした（リボンと折れ線を一緒に描く方法は、標準のリボン グラフにも折れ線および積み上げ縦棒にも無い）
- 横棒でもリボンを出せるようにした。上下に隣り合うカテゴリの同じ系列を帯でつなぐ（これまで横棒のリボンは値の大きい順に積むだけだった）
- 横棒でも「折れ線の値」を描くようにした（標準の複合は縦棒だけ）。既定では線でつながず、カテゴリの中心にマーカーだけを出す。尺度が違えば、値の軸の反対側に第 2 X 軸を出す。ダッシュのマーカーは棒を横切る向き（縦）に引く
- 「線」カードに、標準と同じ「すべての系列に表示」（線ごとは「このシリーズに表示」）を足した。オフにすると線を消してマーカーだけにできる
- 補間の種類がステップの線は、段が変わる位置と同じく、最初と最後のカテゴリの外のすき間の真ん中まで延ばすようにした（プロットの端より外へは出さない）。「段のつなぎを表示」をオフにすると、段と段をつなぐ線を消して、値ごとの水準の線だけにできる（標準に無い項目）。値が 1 つだけの線や、値の無いカテゴリで切れた短い線も、段として見える。網掛け領域も同じ範囲まで塗る
- 書式設定の「列」カードの名前を「棒」にした（縦棒・横棒のどちらでも読めるように。「線」と同じく標準の名前から変えた）。中の「列の色を一致させる」「リボンと列の間のスペース」も「棒」にした。設定はそのまま
- 凡例の印を、棒は棒と同じ色・透過性・罫線・角丸の四角、折れ線は線と同じ色・線のスタイルの短い線（マーカーを出していれば真ん中に重ねる）にした（これまではどれも丸）。横棒では折れ線の印を縦の線にする
- 1.14 までに作ったレポートで見た目が変わるところ：リボンや横棒に「折れ線の値」を入れていた場合、これまで描かなかった線（横棒ではマーカー）と第 2 の値の軸が出る。凡例の印が丸から四角・線に変わる

## 1.14.1.0（2026-09-16）

- 凡例ありのビジュアルで系列の色を 1 つ変えると、触っていない後ろの系列の色がずれていたのを直した（折れ線の色も同じ）
- 軸のタイトルとカテゴリ名を、標準より早く省略していたのを直した。文字幅を実測し、標準と同じ「…」1 文字で省略する。斜めのカテゴリ名は、ビジュアル全体の高さに対する「高さの最大値」まで出す
- 積み上げなどの細い棒でも、文字が入る高さがあればデータ ラベルを出すようにした（標準と同じ。これまでは背景の余白の分まで高さが要った）
- ヘッダーの「…」から、標準と同じく軸や凡例で並べ替えられるようにした
- 書式設定に、グラフの種類とデータに関係ないカードを出さないようにした（標準と同じ）。合計ラベルは積み上げのとき、リボンは縦棒のリボンのとき、第 2 Y 軸・線・網掛け領域・マーカーは折れ線を描くときだけ出す。隠しても設定は残る
- 書式設定に、標準と同じ項目を足した：X 軸タイトルの「スタイル」、グリッド線の「幅で拡大縮小」、合計ラベルの「表示単位」、第 2 Y 軸の範囲の「対数目盛り」「範囲を丸める」「0 を配置する」
- 線に「結合の種類」と「補間の種類」を足した。補間は線形・スムーズ（モノトーン、またはテンションを選べるカーディナル）・ステップ（段の位置を選べる）。線ごとにも変えられる
- マーカーに「型」（円・正方形・ひし形・三角形・バツ・短いダッシュ・長いダッシュ・プラス）、カラー・透過性、罫線を足した。出す・出さないは、標準と同じ「すべてのカテゴリに表示」にした（保存済みの設定はそのまま効く）。大きさも標準に合わせた（サイズ 5 で直径 7.5px。1.10 では 6px）
- 網掛け領域を足した。線と値 0 の高さのあいだを、線の色を 60% 透かして塗る。線ごとに出す・出さないと、塗りの色を選べる
- データ ラベルに「値」と「詳細」の表示を足した。詳細のコンテンツは「全体に対する割合」（標準と同じく小数 2 桁）か「カスタム」（新しいフィールドの枠「ラベルの詳細」に入れた値）で、フォント・カラー・透過性・表示単位・小数点以下の桁数を選べる。値と詳細を両方出すと、値の下に詳細を並べる
- 100% 積み上げのデータ ラベルは、標準と同じく既定で割合（詳細）を出し、値を出さないようにした。1.10.0.0 で作った 100% 積み上げのレポートでデータ ラベルをオンにしていると、表示が値から割合に変わる（「値」をオン、「詳細」をオフにすると元に戻る）

## 1.10.0.0（2026-09-16）

- 系列を複数描けるようにした（集合縦棒）。「凡例」にフィールドを入れるか、「値」に2つ以上入れる
- 凡例（位置12通り・文字・タイトル）。項目のクリックで系列を選べる
- 書式設定「列」の「設定の適用先」に系列が並び、系列ごとに色・透過性・罫線を変えられる。「系列間のスペース」を足した
- データラベルに、系列ごとの「このシリーズに表示」とカラーを足した
- ツールヒントに凡例の値を出す
- 積み上げ縦棒と 100% 積み上げ縦棒。書式設定「列」の「グラフの種類」で切り替える
- 合計ラベル（積み上げ）。「正と負の分割」で、正の合計と負の合計を分けて出せる。合計ラベルを出すときは、軸の端に合計ラベルの入る余白を足す
- 積み上げのツールヒントに合計、100% 積み上げのツールヒントに割合を出す
- 折れ線との複合。「折れ線の値」に入れたメジャーを折れ線で描く。尺度が違えば右の第 2 Y 軸に出す（自動。書式設定「第 2 Y 軸」で出す・出さないを決められる）
- 書式設定に「第 2 Y 軸」（範囲・値・タイトル）、「線」（線ごとの色・幅・線のスタイル）、「マーカー」を足した
- 第 2 Y 軸にも Y 軸と同じ単位の設定（表示単位・単位の表記・軸ラベルに単位を表示・タイトルのスタイル・単位ラベル）を足した。単位ラベルは第 2 Y 軸の上に出す。率のメジャー（書式に % がある）は、表示単位によらず目盛りを % で出す
- 日付のカテゴリをモデルの書式（2025/04 など）で出す
- 横棒。書式設定「列」の「向き」で縦棒と横棒を切り替える（集合・積み上げ・100% 積み上げ）。横棒では、書式設定のカードの名前が標準と同じく入れ替わる（値の軸が「X 軸」）
- リボン。書式設定「列」の「グラフの種類」で選ぶ。カテゴリごとに値の大きい順に上から積み、同じ系列を隣のカテゴリと帯でつなぐ。帯のツールヒントに前後の値・変化・順位を出す。書式設定に「リボン」（色・透過性・罫線・リボンと列の間のスペース）を足した
- 積み上げ・リボンでは、細い棒に入りきらないデータラベルを出さない（上の棒のラベルに重ねない）
- 長い軸のタイトルは、末尾を省略して軸の長さに収める（マウスを乗せると全体が出る）
- 1.4 で作ったレポートはそのまま開ける（値が1つで凡例なしのときの設定は変わらない。見た目は、長い軸のタイトルを省略するところだけ変わる）

## 1.4.5.0（2026-09-15）

- ビジュアルの説明文を変えた（機能と表示の変更はない）

## 1.4.4.0（2026-09-15）

初めての公開版です。

- 表示単位（なし〜兆の13段と自動）、単位の文字、軸の左上の単位ラベル（位置・形・文字サイズ・色）
- 目盛りを数字だけにする表示、英語の表記（K・M・bn・T）への切り替え
- 標準の棒グラフに合わせた書式設定
  - X 軸：値（フォント・色・高さの最大値）、タイトル、カテゴリの最小幅と横スクロール
  - Y 軸：範囲（最小値・最大値・対数目盛り・範囲の反転・範囲を丸める）、値、タイトル、軸の位置の切り替え
  - グリッド線（横・縦）、データラベル、列（色・透過性・罫線・外側のパディング・カテゴリ間のスペース）、棒の最大幅と角丸
- ツールヒント、他のビジュアルからの強調表示、クリックでの選択
