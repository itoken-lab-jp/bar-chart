# 変更履歴

## 1.14.1.0（2026-09-16）

- 凡例ありのビジュアルで系列の色を 1 つ変えると、触っていない後ろの系列の色がずれていたのを直した（折れ線の色も同じ）
- 軸のタイトルとカテゴリ名を、標準より早く省略していたのを直した。文字幅を実測し、標準と同じ「…」1 文字で省略する。斜めのカテゴリ名は、ビジュアル全体の高さに対する「高さの最大値」まで出す
- 積み上げなどの細い棒でも、文字が入る高さがあればデータ ラベルを出すようにした（標準と同じ。これまでは背景の余白の分まで高さが要った）
- ヘッダーの「…」から、標準と同じく軸や凡例で並べ替えられるようにした
- 書式設定に、グラフの種類とデータに関係ないカードを出さないようにした（標準と同じ）。合計ラベルは積み上げのとき、リボンは縦棒のリボンのとき、第 2 Y 軸・線・網掛け領域・マーカーは折れ線を描くときだけ出す。隠しても設定は残る
- 書式設定に、標準と同じ項目を足した：X 軸タイトルの「スタイル」、グリッド線の「幅で拡大縮小」、合計ラベルの「表示単位」、第 2 Y 軸の範囲の「対数目盛り」「範囲を丸める」「0 を配置する」
- 線に「結合の種類」と「補間の種類」を足した。補間は線形・スムーズ（モノトーン、またはテンションを選べるカーディナル）・ステップ（段の位置を選べる）。線ごとにも変えられる
- マーカーに「型」（円・正方形・ひし形・三角形・バツ・短いダッシュ・長いダッシュ・プラス）、カラー・透過性、罫線を足した。出す・出さないは、標準と同じ「すべてのカテゴリに表示」にした（保存済みの設定はそのまま効く）。大きさも標準に合わせた（サイズ 5 で直径 7.5px。1.10 では 6px）
- 網掛け領域を足した。線と値 0 の高さのあいだを、線の色を 60% 透かして塗る。線ごとに出す・出さないと、塗りの色を選べる
- データ ラベルに「値」と「詳細」の表示を足した。詳細のコンテンツは「全体に対する割合」（標準と同じく小数 2 桁）か「カスタム」（新しいフィールドの枠「ラベルの詳細」に入れた値）で、フォント・カラー・透過性・表示単位・小数点以下の桁数を選べる。値と詳細を両方出すと、値の下に詳細を並べる
- 100% 積み上げのデータ ラベルは、標準と同じく既定で割合（詳細）を出し、値を出さないようにした。1.10.0.0 で作った 100% 積み上げのレポートでデータ ラベルをオンにしていると、表示が値から割合に変わる（「値」をオン、「詳細」をオフにすると元に戻る）

## 1.10.0.0（2026-09-16）

- 系列を複数描けるようにした（集合縦棒）。「凡例」にフィールドを入れるか、「値」に2つ以上入れる
- 凡例（位置12通り・文字・タイトル）。項目のクリックで系列を選べる
- 書式設定「列」の「設定の適用先」に系列が並び、系列ごとに色・透過性・罫線を変えられる。「系列間のスペース」を足した
- データラベルに、系列ごとの「このシリーズに表示」とカラーを足した
- ツールヒントに凡例の値を出す
- 積み上げ縦棒と 100% 積み上げ縦棒。書式設定「列」の「グラフの種類」で切り替える
- 合計ラベル（積み上げ）。「正と負の分割」で、正の合計と負の合計を分けて出せる。合計ラベルを出すときは、軸の端に合計ラベルの入る余白を足す
- 積み上げのツールヒントに合計、100% 積み上げのツールヒントに割合を出す
- 折れ線との複合。「折れ線の値」に入れたメジャーを折れ線で描く。尺度が違えば右の第 2 Y 軸に出す（自動。書式設定「第 2 Y 軸」で出す・出さないを決められる）
- 書式設定に「第 2 Y 軸」（範囲・値・タイトル）、「線」（線ごとの色・幅・線のスタイル）、「マーカー」を足した
- 第 2 Y 軸にも Y 軸と同じ単位の設定（表示単位・単位の表記・軸ラベルに単位を表示・タイトルのスタイル・単位ラベル）を足した。単位ラベルは第 2 Y 軸の上に出す。率のメジャー（書式に % がある）は、表示単位によらず目盛りを % で出す
- 日付のカテゴリをモデルの書式（2025/04 など）で出す
- 横棒。書式設定「列」の「向き」で縦棒と横棒を切り替える（集合・積み上げ・100% 積み上げ）。横棒では、書式設定のカードの名前が標準と同じく入れ替わる（値の軸が「X 軸」）
- リボン。書式設定「列」の「グラフの種類」で選ぶ。カテゴリごとに値の大きい順に上から積み、同じ系列を隣のカテゴリと帯でつなぐ。帯のツールヒントに前後の値・変化・順位を出す。書式設定に「リボン」（色・透過性・罫線・リボンと列の間のスペース）を足した
- 積み上げ・リボンでは、細い棒に入りきらないデータラベルを出さない（上の棒のラベルに重ねない）
- 長い軸のタイトルは、末尾を省略して軸の長さに収める（マウスを乗せると全体が出る）
- 1.4 で作ったレポートはそのまま開ける（値が1つで凡例なしのときの設定は変わらない。見た目は、長い軸のタイトルを省略するところだけ変わる）

## 1.4.5.0（2026-09-15）

- ビジュアルの説明文を変えた（機能と表示の変更はない）

## 1.4.4.0（2026-09-15）

初めての公開版です。

- 表示単位（なし〜兆の13段と自動）、単位の文字、軸の左上の単位ラベル（位置・形・文字サイズ・色）
- 目盛りを数字だけにする表示、英語の表記（K・M・bn・T）への切り替え
- 標準の棒グラフに合わせた書式設定
  - X 軸：値（フォント・色・高さの最大値）、タイトル、カテゴリの最小幅と横スクロール
  - Y 軸：範囲（最小値・最大値・対数目盛り・範囲の反転・範囲を丸める）、値、タイトル、軸の位置の切り替え
  - グリッド線（横・縦）、データラベル、列（色・透過性・罫線・外側のパディング・カテゴリ間のスペース）、棒の最大幅と角丸
- ツールヒント、他のビジュアルからの強調表示、クリックでの選択
